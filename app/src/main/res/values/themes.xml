<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CryptographyGallery" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorPrimaryDark">@color/purple_dark</item>
        <item name="colorAccent">@color/light_green_dark</item>
        <item name="colorSecondary">@color/light_green</item>
        <item name="colorSecondaryVariant">@color/light_green_light</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="ThemeOverlay.CryptographyGallery.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_green_dark</item>
        <item name="fullscreenTextColor">@color/light_green</item>
    </style>
</resources>